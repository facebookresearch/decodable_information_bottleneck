model:
  name: vib
  
  architecture:
    is_stochastic: True
    n_test_samples: 12

  loss:
    beta: 1e-3
    regularization_loss: "I[Z;X]"
    is_beta_interpolate : True # interpolate logarithmically with restart every 5 epoch 
    is_beta_interpolate_restart : True 